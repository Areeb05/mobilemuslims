#!/bin/bash
# See scripts/setup-cdn-models.sh — client-side Transformers.js handles model download and cache.

echo "Offline Understand Salah uses Transformers.js; no manual ggml download required."
exit 0
