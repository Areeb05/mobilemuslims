# My V0 Project

Welcome to my project built with Next.js, React, TypeScript, and other modern web technologies.

## Notes for AI Assistants

- **Deployment Environment**: This project is hosted and deployed on Replit, a cloud-based coding platform. When assisting with environment setup or deployment tasks, consider Replit's specific configurations and limitations.
- **Credentials Management**: Sensitive credentials, such as Google API credentials, are stored as secrets in Replit's "Secrets" tab. These are accessible in the application via environment variables (e.g., `process.env.GOOGLE_CREDENTIALS_JSON`). Do not attempt to create or modify `.env.local` files or store sensitive data in the codebase directly.

## Project Overview

[Add other project details, installation instructions, or usage guides here as needed.] 